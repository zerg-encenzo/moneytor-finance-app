import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'moneytor.finance.app',
  appName: 'moneytor-finance-app',
  webDir: 'www'
};

export default config;
